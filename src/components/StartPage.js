import React from "react";

const StartPage = ({ onStart }) => (
  <div>
    <h2>Тест за моделлю Голланда</h2>
    <button onClick={onStart}>Почати тест</button>
  </div>
);

export default StartPage;
